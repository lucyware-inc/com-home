# 의존성
node_modules/

# Cloudflare / Wrangler
.wrangler/
dist/

# 로컬 시크릿 (절대 커밋 금지) — 템플릿(.env.example)만 커밋한다
.dev.vars
.dev.vars.*

# 환경 변수. Hyperdrive 로컬 접속 문자열이 여기 들어간다
.env
.env.*
!.env.example

# OS / 에디터
.DS_Store
Thumbs.db
desktop.ini
.vscode/*
!.vscode/extensions.json
*.log

# Claude Code (개인 설정만 제외, settings.json 은 공유)
.claude/settings.local.json

# 이미지 원본(최적화 전). 배포·커밋에 넣지 않는다
assets/_originals/
