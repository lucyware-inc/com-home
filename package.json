{
  "name": "lucyware-web",
  "version": "1.0.0",
  "private": true,
  "description": "루씨웨어(주) 기업 홈페이지",
  "type": "module",
  "scripts": {
    "dev": "eleventy --serve --port=5820",
    "dev:api": "eleventy && wrangler pages dev --port=8220",
    "build": "eleventy",
    "clean": "node -e \"import('node:fs').then(fs=>fs.rmSync('dist',{recursive:true,force:true}))\""
  },
  "engines": {
    "node": ">=20"
  },
  "devDependencies": {
    "@11ty/eleventy": "^3.0.0",
    "wrangler": "^4.123.0"
  },
  "dependencies": {
    "mysql2": "^3.23.3"
  }
}
