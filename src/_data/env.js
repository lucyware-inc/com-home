/**
 * 빌드 시점에 주입되는 환경 값.
 *
 * Cloudflare Pages 의 Environment variables 가 빌드 명령에 그대로 넘어오므로,
 * 여기서 읽어 템플릿에 넘긴다. **값이 없으면 빈 문자열이고, 그 경우 화면은
 * 그 기능이 없는 상태로 그려진다** — 빌드를 세우지 않는다. 로컬에서 키 없이
 * 빌드하는 일이 흔하고, 그때마다 빌드가 죽으면 아무 작업도 못 한다.
 *
 * `naverMapKeyId` 는 **비밀값이 아니다.** 네이버 지도 스크립트 주소에 그대로
 * 붙어 브라우저로 내려간다. 실제 방어는 숨기는 것이 아니라 네이버 콘솔에
 * 등록한 **Web 서비스 URL 제한**이다 — 그 제한이 있으면 남이 복사해도
 * 우리 도메인 밖에서는 동작하지 않는다. 여기에 두는 이유는 보안이 아니라
 * **저장소가 공개라서** 키를 커밋 이력에 남기지 않으려는 것이고, 키를 바꿀 때
 * 코드를 고치지 않아도 되기 때문이다.
 */
export default {
  naverMapKeyId: process.env.NAVER_MAP_KEY_ID || "",
};
